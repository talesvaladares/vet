{
  "extends": [
    "eslint:recommended",
    "plugin:jest/recommended", //para ele não ficar dando erro no jest
    "next/core-web-vitals",
    "prettier" //para não dar conflito com o prettier
  ],
  "rules": {
    "quotes": ["error", "single", { "allowTemplateLiterals": true }] //faz usar aspas simples no código e permite template literals
  }
}
